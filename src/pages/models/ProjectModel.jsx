class ProjectModel {
  constructor({url, tags, title, description}) {
    this.url = url;
    this.tags = tags;
    this.title = title;
    this.description = description;
  }
}

export default ProjectModel;
