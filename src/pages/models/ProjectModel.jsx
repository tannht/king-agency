class ProjectModel {
  constructor({ id, url, tags, title, description }) {
    this.id = id;
    this.url = url;
    this.tags = tags;
    this.title = title;
    this.description = description;
  }
}

export default ProjectModel;
